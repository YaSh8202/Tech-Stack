import { useContext } from "react";
import { GroupContext } from "../groupContext";

export function useSendMessage({ message, file }) {
  const { selectedGroup } = useContext(GroupContext);
  
  

}
